/**
 * Toggle contact section side panel content.
 * - messageForm: "Send a Message" form (needs working email action)
 * - decorativeCircle: light circle accent (shown when messageForm is false)
 */
export const contactVisibility = {
  messageForm: false,
  decorativeCircle: true,
} as const;
